{
  "name": "common",
  "version": "0.0.1",
  "dependencies": {},
  "scripts": {
    "upload": "git add -A && git commit -m 'add' && git push origin master",
    "download": "git pull origin master",
    "test": "mocha ./json-parser-fp/spec/**/*.spec.js ./json-parser-fp/spec/*.spec.js"
  },
  "devDependencies": {
    "expect": "^23.6.0",
    "mocha": "^5.2.0"
  }
}
