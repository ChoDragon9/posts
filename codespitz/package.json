{
  "name": "common",
  "version": "0.0.1",
  "dependencies": {},
  "scripts": {
    "upload": "git add -A && git commit -m 'add' && git push origin master",
    "download": "git pull origin master",
    "test": "mocha ./json-parser-fp3/spec/**/*.spec.js ./json-parser-fp3/spec/*.spec.js"
  },
  "devDependencies": {
    "expect": "^29.7.0",
    "mocha": "^5.2.0"
  }
}
