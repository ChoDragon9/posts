[서적] 프로그램 디자인, 어떻게 할 것인가

- 이터레이터
  - done이 true일 때까지 반복한다.
  - 그럼 다양한 함수를 만들 수 있을 듯
  - 언어를 다룰 수 있는 하나의 포인트
- reduce 동의기능 foldLeft, foldRight
- 일급 함수, 클로저가 함수형의 기본
- flat
  - flatMap = pipe(L.map, flat)
- 언어를 많드는 것